# 项目架构导读

如果你第一次读这个仓库，不要先钻进某个模型文件里。这个项目真正想教会你的，不只是某一个网络怎么写，而是一个深度学习项目通常会分成哪几层，每一层分别承担什么职责。

## 一句话理解这个仓库

这是一个“配置驱动”的训练框架。

它用同一套训练入口，串起了六条学习主线：

- CNN / ResNet 分类
- VAE 生成
- DDPM 扩散
- 超分基线
- SR3 条件扩散超分
- ResShift 风格的条件扩散超分

换句话说，项目想让你看到的是：

同一套工程骨架，可以承载不同任务；变化的主要是数据定义、模型结构和训练目标。

## 配套知识文档

建议把这几份文档配合着看：

- `docs/architecture.md`：讲项目分层和调用链
- `docs/learning_path.md`：讲整体顺序和每个阶段应该看什么
- `docs/generative_basics.md`：讲 VAE、DDPM、SR3、ResShift 的核心概念
- `docs/resshift.md`：专门讲简化版 ResShift 的定位和思路

## 先记住四层结构

### 1. `configs/`：实验说明书

配置文件决定这次实验要做什么。

比如一份配置通常会回答这些问题：

- 训练多少轮
- 学习率是多少
- 用哪个模型
- 用哪个 trainer
- 数据集是什么
- 图片范围是 `[0, 1]` 还是 `[-1, 1]`

你可以把配置理解成“给程序看的实验计划表”。

### 2. `main.py`：总入口

`main.py` 本身不做复杂训练，它主要负责：

1. 读取配置
2. 设置随机种子
3. 创建 dataloader
4. 根据 `trainer_name` 找到对应 trainer
5. 调用 `trainer.fit()`

这说明入口文件越薄越好。复杂逻辑如果都堆在入口里，后面一扩展任务就会很乱。

### 3. `trainer/`：训练流程层

trainer 是整个项目最关键的抽象层。

它负责的是“怎么训练”，而不是“网络长什么样”。

以 `BaseTrainer` 为例，它负责统一处理：

- 设备选择
- 模型构建
- 优化器构建
- checkpoint 保存
- 训练循环
- 验证循环
- 最优指标监控

而具体任务只需要覆写少量 hook：

- 分类任务默认使用基类
- VAE 改 loss 和验证指标
- DDPM 改时间步训练逻辑
- SR / SR3 / ResShift 改输入输出定义和评估指标

这里的设计重点是：

把“通用流程”放在父类，把“任务差异”放在子类。

### 4. `models/`：网络定义层

model 的职责应该尽量单纯：

- 接受输入
- 前向传播
- 输出预测结果

model 不应该知道：

- checkpoint 怎么保存
- dataloader 怎么构造
- 实验目录叫什么
- 这次训练的最佳指标是多少

这类事情应该归 trainer 管。

## 一次训练到底是怎么跑起来的

推荐你按下面顺序跟代码：

1. 打开 `configs/...yaml`
2. 看 `main.py`
3. 看 `trainer/__init__.py`
4. 看具体的 trainer 文件
5. 再去看对应 model
6. 最后回来看 `data/` 怎么准备样本

把这条链路串起来，你就能真正理解项目，而不是只会“跑命令”。

## 数据层在教什么

这个项目的数据层刻意保持得比较直观。

### 标准任务

分类、VAE、DDPM 这些任务默认还是读取普通图像数据集，比如 `MNIST`。

原始样本长这样：

- 分类：`(image, label)`
- 生成：`(image, label)`，虽然 label 通常不用

### 超分任务

超分不是直接拿分类数据来训，而是先把原始图像包装成新的训练对：

- 输入：`lr_up`
- 目标：`hr`

也就是：

1. 从原始数据集中拿到高分辨率图 `hr`
2. 对它做下采样，得到低分图 `lr`
3. 再把 `lr` 插值回原尺寸，得到 `lr_up`
4. 返回 `(lr_up, hr)`

这里最重要的工程思想是：

原始数据集和任务样本不是一回事。

同一个基础数据集，可以被包装成分类任务、生成任务、超分任务等不同训练样本。

## 五条主线各自教什么

### 1. CNN / ResNet

这是最基础的一条线，用来理解：

- 配置驱动训练入口
- 标准监督学习循环
- 分类指标怎么计算

### 2. VAE

这条线帮助你理解：

- 模型输出不一定只有一个张量
- loss 也不一定是单一项
- 验证指标有时是越小越好，而不是越大越好
- 生成模型为什么要学习潜空间而不只是记忆输入

### 3. DDPM

这里开始引入“时间步”。

重点不是先背公式，而是先理解训练对象变了：

- 输入不再只是原图
- 模型需要看到时间步 `t`
- 目标也不再是类别，而是噪声预测
- 采样过程本身也是模型设计的一部分

### 4. SR 基线

这条线帮助你理解“图像恢复”和“图像分类”的差别。

分类看的是类别是否正确。

超分看的是图像是否恢复得更接近真值，所以常用：

- L1 / L2 作为训练损失
- PSNR 作为验证指标

### 5. ResShift

这是本项目最适合继续往扩散恢复方向延伸的一条扩展线。

它把条件恢复任务和扩散思想结合起来，但又不要求一开始就理解完整 DDPM 体系。

你可以把它理解成：

- SRResNet：一步直接修图
- ResShift：带时间步、少步数地逐步恢复残差

在它之前，先看 SR3，会更容易理解“条件扩散超分”。

如果你对这些概念还不熟，建议直接接着看 `docs/generative_basics.md` 和 `docs/learning_path.md`。

## 这次结构更新解决了什么问题

### 问题 1：不同任务的“最佳指标”逻辑分散

以前分类看 `Acc`，VAE 看 `Loss`，SR 看 `PSNR`，每个 trainer 都各写一套 best/save 逻辑。

这会带来两个问题：

- 重复代码多
- 容易出现某个任务保存逻辑和基类不一致

现在统一改成了“监控指标”机制：

- 每个 trainer 只声明自己监控什么指标
- 基类统一负责比较、保存 best、保存 last

这也是实际工程里常见的重构方向。

### 问题 2：DDPM 的 checkpoint 语义不清楚

以前 DDPM 为了兼容分类 trainer，会返回一个假的指标值去触发保存。

这样语义会更清楚。

现在 DDPM 会：

- 训练时记录噪声预测损失
- 验证时计算 `Val Noise Loss`
- 同时额外保存采样图片

这样“指标”和“可视化”各司其职，更容易理解。

### 问题 3：数据层文件职责太重

以前 `data/dataloader.py` 同时负责：

- transform
- SR 包装数据集
- dataset registry
- dataloader 构造

现在把最容易讲解的两部分拆了出来：

- `data/transforms.py`
- `data/sr_dataset.py`

这样看数据层时，不会一上来就被一个大文件淹没。

## 建议的阅读顺序

推荐这样看：

1. `configs/classification/cnn.yaml`
2. `main.py`
3. `trainer/base.py`
4. `models/cnn.py`
5. `trainer/vae.py`
6. `trainer/diffusion.py`
7. `data/sr_dataset.py`
8. `trainer/sr.py`
9. `trainer/resshift.py`
10. `models/resshift.py`

顺序不要反过来。先看流程，再看模型，效率会高很多。

## 可以继续做的扩展练习

如果你已经读懂这套结构，可以尝试下面几个练习：

1. 给分类任务增加 `FashionMNIST` 配置。
2. 给 `BaseTrainer` 增加学习率调度器支持。
3. 给超分任务增加 `SSIM` 指标。
4. 把 DDPM 的采样图保存频率做成可配置项。
5. 增加一个新的 trainer，比如去噪任务。

真正学会工程，不是把现有代码跑通，而是你能在现有结构上继续扩展。
